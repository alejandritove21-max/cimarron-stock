{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/notion-proxy/(.*)", "destination": "/api/notion-proxy/$1" },
    { "source": "/(.*)",              "destination": "/index.html" }
  ]
}
